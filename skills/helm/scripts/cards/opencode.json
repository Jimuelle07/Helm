{
  "name": "opencode",
  "display_name": "OpenCode",
  "bin_names": [
    "opencode"
  ],
  "competence": "Provider-agnostic terminal agent; the right pick when a specific non-default model or a self-hosted provider has to be used",
  "strengths": [
    "swaps model providers freely",
    "can drive local/self-hosted endpoints",
    "lists its own available models"
  ],
  "weaknesses": [
    "quality tracks whichever backing model is configured",
    "smaller ecosystem than the first-party CLIs"
  ],
  "task_fit": {
    "scaffold": 3,
    "feature": 4,
    "bugfix": 4,
    "refactor": 3,
    "test": 3,
    "docs": 3,
    "review": 3,
    "research": 3,
    "ops": 3,
    "other": 3
  },
  "context_class": "large",
  "auth": {
    "modes": [
      "api_key",
      "oauth"
    ],
    "env": [
      "OPENAI_API_KEY",
      "ANTHROPIC_API_KEY",
      "OPENROUTER_API_KEY"
    ],
    "config": [
      "~/.config/opencode"
    ]
  },
  "headless": {
    "argv": [
      "opencode",
      "run",
      "--auto",
      "{flags}",
      "{prompt}"
    ],
    "model_flag": "--model",
    "cwd_aware": true,
    "write_enabled": true,
    "modes": {
      "unattended": {
        "satisfied_by_base": true,
        "verified": true,
        "note": "`--auto` in the base contract already auto-approves permissions",
        "source": "--help, verified on this machine 2026-09-20"
      },
      "plan": {
        "prompt_prefix": "Before writing any code, work out the structure: which files to create, what each is responsible for, and the interfaces between them. State that plan, then implement it. Task: ",
        "verified": true,
        "source": "CLI Coding Agents Cheat Sheet"
      },
      "parallel": {
        "prompt_prefix": "This change spans many files. Work through them in independent batches rather than one long serial pass, and keep a checklist of what is done. Task: ",
        "verified": true,
        "source": "CLI Coding Agents Cheat Sheet"
      },
      "readonly": {
        "prompt_prefix": "Do not modify, create, or delete any file. Read, analyse, and report your findings only. Task: ",
        "verified": true,
        "note": "advisory only -- opencode has no read-only flag, so this is an instruction the model can disregard",
        "source": "CLI Coding Agents Cheat Sheet"
      }
    },
    "recovery": {
      "no_op": {
        "prompt_prefix": "Your previous run described this work instead of doing it. Do not restate the plan -- make the edits on disk now, and finish by listing the files you changed. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "re-dispatch with an explicit apply-the-edits instruction"
      },
      "stuck": {
        "prompt_prefix": "You are running headless: nobody can answer a question, and asking one ends the run with nothing done. Make reasonable assumptions, complete the task, and record those assumptions in your final message. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "re-dispatch telling the agent no answer can arrive"
      },
      "failed": {
        "prompt_prefix": "A previous attempt at this task errored out. Inspect the working tree for half-applied changes, clean them up, then complete the task. Task: ",
        "note": "opencode has /undo (Ctrl+X U) to revert the last tool execution, but only interactively -- whether `opencode run` treats a leading /undo as a command or as a prompt is unconfirmed, so it is not wired up as a cleanup"
      }
    }
  },
  "list_models": [
    "opencode",
    "models"
  ],
  "contract_verified": true,
  "auth_check": {
    "argv": [
      "opencode",
      "auth",
      "list"
    ],
    "ok_pattern": "[1-9]\\d*\\s+credentials?",
    "fail_pattern": "\\b0\\s+credentials?\\b",
    "timeout": 40
  }
}
