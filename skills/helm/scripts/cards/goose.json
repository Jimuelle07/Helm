{
  "name": "goose",
  "display_name": "Goose",
  "bin_names": [
    "goose"
  ],
  "competence": "Extension-driven automation where the task needs external systems wired in via MCP rather than pure code editing",
  "strengths": [
    "strong MCP/extension ecosystem",
    "provider-agnostic",
    "good at multi-system glue work"
  ],
  "weaknesses": [
    "setup-heavy for simple edits",
    "quality tracks the configured backing model"
  ],
  "task_fit": {
    "scaffold": 3,
    "feature": 3,
    "bugfix": 3,
    "refactor": 2,
    "test": 2,
    "docs": 2,
    "review": 2,
    "research": 3,
    "ops": 5,
    "other": 3
  },
  "context_class": "large",
  "auth": {
    "modes": [
      "api_key"
    ],
    "env": [
      "OPENAI_API_KEY",
      "ANTHROPIC_API_KEY"
    ],
    "config": [
      "~/.config/goose"
    ]
  },
  "headless": {
    "argv": [
      "goose",
      "run",
      "{flags}",
      "-t",
      "{prompt}"
    ],
    "model_flag": null,
    "cwd_aware": true,
    "modes": {
      "unattended": {
        "satisfied_by_base": true,
        "verified": false,
        "note": "`goose run` is the documented headless, non-interactive batch form",
        "source": "CLI Coding Agents Cheat Sheet"
      },
      "plan": {
        "prompt_prefix": "Work out the structure first -- which files to create, what each is responsible for, and the interfaces between them -- then implement it. Task: ",
        "verified": true,
        "note": "deliberately NOT /plan: that directive is read-only until /endplan, which is unreachable in a single headless run",
        "source": "CLI Coding Agents Cheat Sheet"
      },
      "parallel": {
        "prompt_prefix": "This change spans many files. Work through them in independent batches rather than one long serial pass, and keep a checklist of what is done. Task: ",
        "verified": true,
        "source": "CLI Coding Agents Cheat Sheet"
      },
      "readonly": {
        "prompt_prefix": "/plan ",
        "verified": true,
        "note": "/plan locks goose into a read-only architectural phase; it stays there until /endplan, which a one-shot headless run never gets to send",
        "source": "CLI Coding Agents Cheat Sheet"
      }
    },
    "recovery": {
      "no_op": {
        "prompt_prefix": "Your previous run described this work instead of doing it. Do not restate the plan -- make the edits on disk now, and finish by listing the files you changed. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "re-dispatch with an explicit apply-the-edits instruction"
      },
      "stuck": {
        "prompt_prefix": "You are running headless: nobody can answer a question, and asking one ends the run with nothing done. Make reasonable assumptions, complete the task, and record those assumptions in your final message. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "re-dispatch telling the agent no answer can arrive"
      },
      "failed": {
        "prompt_prefix": "A previous attempt at this task errored out. Inspect the working tree for half-applied changes, clean them up, then complete the task. Task: ",
        "note": "re-dispatch after checking the tree for partial work"
      }
    }
  },
  "contract_verified": false,
  "card_note": "goose treats `stop`, `halt`, `cancel`, `wait` and `hold on` as interrupt keywords that abort the active tool loop, and `actually`, `instead`, `change of plans` as pivot keywords. A task phrased with one of those may halt the run."
}
