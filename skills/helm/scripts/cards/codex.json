{
  "name": "codex",
  "display_name": "OpenAI Codex CLI",
  "bin_names": [
    "codex"
  ],
  "competence": "Well-scoped implementation and debugging inside a codebase that already exists, especially iterating against a failing test until it passes",
  "strengths": [
    "tight implement-run-fix loop",
    "good at reading stack traces",
    "selectable reasoning effort",
    "sandboxed execution"
  ],
  "weaknesses": [
    "prefers a concrete target over an open brief",
    "less suited to greenfield architecture"
  ],
  "task_fit": {
    "scaffold": 3,
    "feature": 4,
    "bugfix": 5,
    "refactor": 4,
    "test": 5,
    "docs": 3,
    "review": 4,
    "research": 2,
    "ops": 3,
    "other": 3
  },
  "context_class": "large",
  "auth": {
    "modes": [
      "subscription",
      "api_key"
    ],
    "env": [
      "OPENAI_API_KEY"
    ],
    "config": [
      "~/.codex/auth.json"
    ]
  },
  "headless": {
    "argv": [
      "codex",
      "exec",
      "--sandbox",
      "workspace-write",
      "{flags}",
      "{prompt}"
    ],
    "model_flag": "--model",
    "cwd_aware": true,
    "write_enabled": true,
    "modes": {
      "unattended": {
        "satisfied_by_base": true,
        "verified": true,
        "note": "`codex exec` is non-interactive by construction; it never prompts"
      },
      "sandbox": {
        "satisfied_by_base": true,
        "verified": true,
        "note": "`--sandbox workspace-write` in the base contract already confines writes"
      },
      "readonly": {
        "args": [
          "--sandbox",
          "read-only"
        ],
        "replaces": [
          "--sandbox",
          "-s"
        ],
        "verified": true,
        "note": "downgrades the sandbox so the run cannot write at all"
      },
      "plan": {
        "prompt_prefix": "Before writing any code, work out the structure: which files to create, what each is responsible for, and the interfaces between them. State that plan, then implement it. Task: ",
        "verified": true
      },
      "parallel": {
        "prompt_prefix": "This change spans many files. Work through them in independent batches rather than one long serial pass, and keep a checklist of what is done. Task: ",
        "verified": true
      }
    },
    "recovery": {
      "no_op": {
        "prompt_prefix": "Your previous run described this work instead of doing it. Do not restate the plan -- make the edits on disk now, and finish by listing the files you changed. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "re-dispatch with an explicit apply-the-edits instruction"
      },
      "stuck": {
        "prompt_prefix": "You are running headless: nobody can answer a question, and asking one ends the run with nothing done. Make reasonable assumptions, complete the task, and record those assumptions in your final message. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "re-dispatch telling the agent no answer can arrive"
      },
      "failed": {
        "prompt_prefix": "A previous attempt at this task errored out. Inspect the working tree for half-applied changes, clean them up, then complete the task. Task: ",
        "note": "re-dispatch after checking the tree for partial work"
      }
    }
  },
  "contract_verified": true,
  "auth_check": {
    "argv": [
      "codex",
      "login",
      "status"
    ],
    "ok_pattern": "logged in",
    "fail_pattern": "not logged in|no credentials|please run .*login",
    "timeout": 30
  }
}
