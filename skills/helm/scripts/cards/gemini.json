{
  "name": "gemini",
  "display_name": "Gemini CLI",
  "bin_names": [
    "gemini"
  ],
  "competence": "Very large context sweeps: reading, summarising or auditing big files or whole directories in a single pass",
  "strengths": [
    "very large context window",
    "cheap on long inputs",
    "good at summarising sprawling material",
    "generous free tier"
  ],
  "weaknesses": [
    "less reliable at long multi-step edit loops",
    "weaker agentic tool discipline than peers"
  ],
  "task_fit": {
    "scaffold": 3,
    "feature": 3,
    "bugfix": 3,
    "refactor": 3,
    "test": 3,
    "docs": 5,
    "review": 4,
    "research": 5,
    "ops": 2,
    "other": 3
  },
  "context_class": "xlarge",
  "auth": {
    "modes": [
      "oauth",
      "api_key"
    ],
    "env": [
      "GEMINI_API_KEY",
      "GOOGLE_API_KEY"
    ],
    "config": [
      "~/.gemini"
    ]
  },
  "headless": {
    "argv": [
      "gemini",
      "--approval-mode",
      "auto_edit",
      "{flags}",
      "-p",
      "{prompt}"
    ],
    "model_flag": "--model",
    "cwd_aware": true,
    "write_enabled": true,
    "modes": {
      "unattended": {
        "args": [
          "--approval-mode",
          "yolo"
        ],
        "replaces": [
          "--approval-mode"
        ],
        "verified": true,
        "note": "auto-approves every tool, not just edits"
      },
      "sandbox": {
        "args": [
          "-s"
        ],
        "verified": true,
        "note": "runs tools inside Gemini's sandbox; -w for a git worktree instead"
      },
      "readonly": {
        "args": [
          "--approval-mode",
          "plan"
        ],
        "replaces": [
          "--approval-mode"
        ],
        "verified": true,
        "note": "plan mode is read-only"
      },
      "plan": {
        "prompt_prefix": "Before writing any code, work out the structure: which files to create, what each is responsible for, and the interfaces between them. State that plan, then implement it. Task: ",
        "verified": true
      },
      "parallel": {
        "prompt_prefix": "This change spans many files. Work through them in independent batches rather than one long serial pass, and keep a checklist of what is done. Task: ",
        "verified": true
      }
    },
    "recovery": {
      "no_op": {
        "prompt_prefix": "Your previous run described this work instead of doing it. Do not restate the plan -- make the edits on disk now, and finish by listing the files you changed. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "re-dispatch with an explicit apply-the-edits instruction"
      },
      "stuck": {
        "prompt_prefix": "You are running headless: nobody can answer a question, and asking one ends the run with nothing done. Make reasonable assumptions, complete the task, and record those assumptions in your final message. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "`/reset` is a session command and `-p` already starts a fresh session every run, so there is no stale history to clear -- the fix here is telling it not to ask, not resetting it"
      },
      "failed": {
        "prompt_prefix": "A previous attempt at this task errored out. Inspect the working tree for half-applied changes, clean them up, then complete the task. Task: ",
        "note": "re-dispatch after checking the tree for partial work"
      }
    }
  },
  "contract_verified": true
}
