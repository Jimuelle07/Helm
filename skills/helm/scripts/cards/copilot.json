{
  "name": "copilot",
  "display_name": "GitHub Copilot CLI",
  "bin_names": [
    "copilot"
  ],
  "competence": "GitHub-centric work: issues, pull requests, Actions workflows, and following an existing repository's established conventions",
  "strengths": [
    "native GitHub context (issues, PRs, CI)",
    "good at matching house style",
    "included with a Copilot seat"
  ],
  "weaknesses": [
    "least useful away from a GitHub remote",
    "more conservative on large refactors"
  ],
  "task_fit": {
    "scaffold": 3,
    "feature": 4,
    "bugfix": 4,
    "refactor": 3,
    "test": 4,
    "docs": 4,
    "review": 5,
    "research": 2,
    "ops": 4,
    "other": 3
  },
  "context_class": "medium",
  "auth": {
    "modes": [
      "subscription",
      "oauth"
    ],
    "env": [
      "GITHUB_TOKEN",
      "GH_TOKEN"
    ],
    "config": [
      "~/.copilot"
    ]
  },
  "headless": {
    "argv": [
      "copilot",
      "--allow-all-tools",
      "{flags}",
      "-p",
      "{prompt}"
    ],
    "model_flag": "--model",
    "cwd_aware": true,
    "write_enabled": true,
    "modes": {
      "unattended": {
        "args": [
          "--allow-all"
        ],
        "replaces": [
          "--allow-all-tools"
        ],
        "verified": true,
        "note": "--allow-all is --allow-all-tools plus paths and URLs",
        "source": "--help, verified on this machine 2026-09-20"
      },
      "readonly": {
        "args": [
          "--mode",
          "plan"
        ],
        "replaces": [
          "--allow-all-tools"
        ],
        "verified": true,
        "note": "plan mode, with the tool grant removed so it cannot write",
        "source": "--help, verified on this machine 2026-09-20"
      },
      "plan": {
        "prompt_prefix": "Before writing any code, work out the structure: which files to create, what each is responsible for, and the interfaces between them. State that plan, then implement it. Task: ",
        "verified": true,
        "source": "CLI Coding Agents Cheat Sheet"
      },
      "parallel": {
        "prompt_prefix": "This change spans many files. Work through them in independent batches rather than one long serial pass, and keep a checklist of what is done. Task: ",
        "verified": true,
        "source": "CLI Coding Agents Cheat Sheet"
      }
    },
    "recovery": {
      "no_op": {
        "prompt_prefix": "Your previous run described this work instead of doing it. Do not restate the plan -- make the edits on disk now, and finish by listing the files you changed. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "re-dispatch with an explicit apply-the-edits instruction"
      },
      "stuck": {
        "prompt_prefix": "You are running headless: nobody can answer a question, and asking one ends the run with nothing done. Make reasonable assumptions, complete the task, and record those assumptions in your final message. Task: ",
        "modes": [
          "unattended"
        ],
        "note": "re-dispatch telling the agent no answer can arrive"
      },
      "failed": {
        "prompt_prefix": "A previous attempt at this task errored out. Inspect the working tree for half-applied changes, clean them up, then complete the task. Task: ",
        "note": "re-dispatch after checking the tree for partial work"
      }
    }
  },
  "contract_verified": true,
  "card_note": "The cheat sheet's Copilot section documents the older `gh copilot suggest/explain` extension. This card targets the standalone GitHub Copilot CLI (1.0.83 when verified), which is a different tool: -p/--prompt, --allow-all-tools, --mode plan."
}
